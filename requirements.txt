# Runtime dependencies only — installed into the production Docker image.
# Test/lint tooling lives in requirements-dev.txt so the prod image stays lean.
# Versions pinned exactly for reproducible builds and supply-chain hygiene.
flask==3.1.0
gunicorn==23.0.0
psycopg2-binary==2.9.10
redis==5.2.1
